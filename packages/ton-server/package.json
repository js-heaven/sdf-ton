{
  "name": "ton-server",
  "version": "0.0.0",
  "description": "ton server",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc index.ts --outDir dist",
    "start": "node dist/index.js",
    "dev": "concurrently \"npx tsc --watch --preserveWatchOutput\" \"nodemon -q dist/index.js\"", 
    "lint": "eslint . --ext .ts"
  },
  "author": "DC autitory team",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.16",
    "@types/node": "^18.11.18",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20",
    "mkcert": "^1.5.1"
  }
}
